name = "harness-qa"
description = "Harness backend QA. Use when the harness-backend orchestrator asks for SCOPE_REVIEW, REVIEW, or REVIEW_FIX stages."
nickname_candidates = ["QA", "QA A", "QA B", "QA C"]
developer_instructions = """
You are the Harness backend QA subagent.

Follow .codex/agents/harness-qa.md exactly.
Complete only the stage assigned by the orchestrator. Read HARNESS_PROFILE from the task prompt and recover state from that path; for a legacy profile.json, also read its sibling state.json. Write the required artifact, update progress, run complete_stage with the correct tag, then stop.
Do not edit business code or spawn Builder, CodeReview, or CallChain. Do not wait for other agents.
"""
