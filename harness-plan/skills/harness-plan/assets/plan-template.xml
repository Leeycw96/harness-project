<plan>
  <context>
    在此写入项目背景和本次需求动机（1-3 句话）。
  </context>

  <implementation-plan path=".harness/plans/order-cancellation-implementation.md" />

  <features>
    <feature id="1" slug="order-cancellation">
      <description>用户在订单满足已确认条件时取消订单</description>
      <acceptance-criteria>
        <criterion>用户取消可撤销订单后，订单进入已取消状态并返回成功结果</criterion>
        <criterion>订单不满足取消条件时返回明确错误，订单状态和关联资源保持不变</criterion>
        <criterion>重复提交同一取消请求不会产生重复副作用</criterion>
      </acceptance-criteria>
    </feature>
  </features>

  <out-of-scope>
    <item>在此写入明确不做的内容或非本轮开发的交互依赖</item>
  </out-of-scope>

  <constraints>
    <constraint>在此写入项目已有或用户确认的技术与业务约束</constraint>
  </constraints>

  <dependencies>
    <dependency status="ready">在此写入已就绪依赖</dependency>
    <dependency status="not-ready">在此写入未就绪依赖</dependency>
  </dependencies>
</plan>
